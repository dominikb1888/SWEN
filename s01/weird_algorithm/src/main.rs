fn main() {
    // Read from stdin and write to stdout
    let input = std::io::read_to_string(std::io::stdin()).unwrap();
    let n = input.parse::<i32>();

    let numbers: Vec<i32> = vec![n];
    loop {

        if n % 2 == 0 {
            
        } else {
        
        }

        if n == 1 {
            break;
        }
    }
    
    let result: String = numbers.join(" ").collect();
    println!("{}", ???)

}
